#!/usr/bin/env bash
plugin_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
(cd $plugin_dir && ansible-playbook site.yml -e "ghc_version=${ASDF_INSTALL_VERSION} ghc_prefix_dir=${ASDF_INSTALL_PATH}")
